文献模块：释文改为整篇文章阅读 / 编辑，关联操作栏固定在右栏顶部

- 总述与各层释文连成一篇（层名为行内小标题），不再分多个文本框；「编辑全文」整篇一个文本框，
  以 "## 第N层 · 层名" 分隔，保存时按层拆回（可改层名，不能增减层）
- 右栏顶部固定操作栏：显示选中节点 / 已选文字，「关联到本节点」「解除」「编辑全文」不随滚动移动
- 石头元数据收进可折叠块；后端 PATCH layers 支持改层名
